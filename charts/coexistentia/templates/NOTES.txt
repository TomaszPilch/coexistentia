1. Get the application URL:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else }}
  export POD_NAME=$(kubectl get pods --namespace {{ include "coexistentia.namespace" . }} -l "app.kubernetes.io/name={{ include "coexistentia.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ include "coexistentia.namespace" . }} port-forward "$POD_NAME" 8080:{{ .Values.service.targetPort }}
  echo "Open http://127.0.0.1:8080"
{{- end }}
